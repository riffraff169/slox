fun add(last, el) { return last + el; }
fun mul(last, el) { return last * el; }
fun div(last, el) { return last / el; }
print [1,2,3].reduce(0, add);
print [1,2,3].reduce(2, mul);
print [2,2,1].reduce(4, div);

__END__
-- expect: --
6
12
1
// expect: 6
// expect: 12
// expect: 1
