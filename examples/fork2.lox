var forkres = Process.fork().unwrap();

if (forkres == 0) {
  for (var i = 0; i < 1000; i += 1) {
    print "[Child] ====> ", i;
  }
  System.exit(0);
} else {
  for (var j = 0; j < 1000; j += 1) {
    print "[Parent] ", j;
  }
}
